// important file when deploying to a subcategory
import { defineConfig } from 'vite' 

export default defineConfig({
  // base: '/todo/',
  // other options
})

//uncomment the base option and set it to '/todo/' to deploy to a subcategory